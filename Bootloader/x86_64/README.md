# AWEOS x86_64 Bootloader

Architecture slot for the Limine bootloader used by AWEOS.

Upstream: https://github.com/Limine-Bootloader/Limine

The AWEOS Linux kernel remains under `/linux` and is not modified by the bootloader synchronization workflow.